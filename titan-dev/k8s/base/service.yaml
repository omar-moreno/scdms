apiVersion: v1
kind: Service
metadata:
  name: apache
  labels:
    name: apache
spec:
  type: NodePort
  selector:
    app: titan-http-server
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
